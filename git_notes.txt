git config (configures)
git config --list (checks user and email)
git status (checks status)
git add    (stages)
git commit -m"notes" (commits don't foget the notes)
git log  (gets hash to check)
git branch (creates new branch)
git checkout (switches)
touch (created empty file for new branch)
ls -la (shows all directories, even hidden ones)

remote for Github
local for laptop

Save file to computer!!!

git remove -v (two repositories communicating)
push or pull to web

git remote add origin https....... (from github. use https)
git push -v origin master (both of these lines are on github)